<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theoakteam.ramadanapp" >
<uses-permission android:name="com.android.alarm.permission.SET_ALARM">

</uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity.MainSplashScreen"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SehriAndIfterShortForm"
            android:label="@string/title_activity_sehri_and_ifter_short_form" />
        <activity
            android:name=".DisplaySuraActivity"
            android:label="@string/title_activity_quran" />
        <activity
            android:name=".TasbhiActivity1"
            android:label="@string/title_activity_tasbih" >
        </activity>
        <activity android:name=".SetingsActivity"
            android:label="@string/title_settings"></activity>

        <service android:name=".NotifyService"></service>


    </application>

</manifest>
